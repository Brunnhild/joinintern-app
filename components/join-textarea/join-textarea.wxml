<!--components/join-textarea/join-textarea.wxml-->
<view>
  <text class="{{showBullet ? 'prepend-bullet' : ''}} input-title">{{title}}</text>
  <textarea maxlength="-1" class="rounded-textarea" bindinput="input" placeholder="{{placeholder}}"></textarea>
</view>
